name: offline_first_cache_example
description: Demonstrates the usage of offline_first_cache package.
publish_to: 'none'

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  offline_first_cache:
    path: ../
  dio: ^5.0.0
  uuid: ^4.5.2
  connectivity_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
